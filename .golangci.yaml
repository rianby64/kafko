linters:
  presets:
    - import
    - metalinter
    - bugs
    - complexity
    - error
    - module
    - style
    - performance
    - test
    - unused
  disable:
    - exhaustruct
    - lll
    - tagliatelle
    - depguard
  # Run only fast linters from enabled linters set (first run won't be fast)
  # Default: false
  fast: false
